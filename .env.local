# MongoDB Connection URI
# Replace <db_password> with your actual password and <database_name> with your database name (e.g., pos_db)
MONGODB_URI="mongodb+srv://testuser:PaanBatta123%21%40%23@cluster0.jryy5fp.mongodb.net/?appName=Cluster0"

# NextAuth Configuration
# Run 'openssl rand -base64 32' in your terminal to generate a strong secret
NEXTAUTH_SECRET="your_nextauth_secret_here"
NEXTAUTH_URL="http://localhost:3000"

# Application Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
